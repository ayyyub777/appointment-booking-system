export const publicRoutes = ["/", "/confirm-email"];

export const authRoutes = ["/login", "/register", "/reset-password"];

export const authApi = "/api/auth";

export const postSigninRedirect = "/app";
